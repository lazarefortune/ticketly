<incremental-input
        name="{{ full_name }}"
        min="{{ attr.min|default(0) }}"
        max="{{ attr.max|default(99999) }}"
        step="{{ attr.step|default(1) }}"
        value="{{ value|default(attr.min|default(0)) }}"
></incremental-input>