{% extends 'admin/base.html.twig' %}

{% block title %}Coupon - Création{% endblock %}

{% block body %}

    <div>
        <div>
            <a class="inline-flex items-center gap-2 text-sm/6 text-zinc-500 dark:text-zinc-400"
               href="{{ path('admin_coupon_index') }}">
                {{ icon('chevron-left') }}
                Retour
            </a>
        </div>

        <div class="mt-6">
            {% include 'admin/coupons/_form.html.twig' with {'form': form} %}
        </div>
    </div>

{% endblock %}


