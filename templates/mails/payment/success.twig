{% extends layout %}

{% import "mails/macros.twig" as e %}

{% block body %}

    {% apply markdown_email %}

        ## Bonjour {{ reservation.name }},

        Votre paiement de {{ reservation.totalAmount|price_format }} a bien été enregistré pour notre événement : <b>{{ reservation.event.name }}</b>.

        Vous pouvez consulter votre réservation en cliquant sur le bouton ci-dessous.

    {% endapply %}

    {{ e.button('Vos billets', reservationUrl) }}

    {% apply markdown_email %}

        Merci de votre confiance et à bientôt.

    {% endapply %}

    {{ e.signature(app_name) }}

{% endblock %}