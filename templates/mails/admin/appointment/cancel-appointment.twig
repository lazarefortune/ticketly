{% extends layout %}

{% import "mails/macros.twig" as e %}

{% block body %}

    {% apply markdown_email %}

        ## Bonjour,

        Le rendez-vous de {{ appointment.client.fullname }}
        prévu le {{ appointment.date|date('d/m/Y') }}
        entre {{ appointment.startTime|date('H:i') }} et {{ appointment.endTime|date('H:i') }}
        a bien été annulé.

    {% endapply %}

    {{ e.button('Consulter les détails', appointment_url) }}

{% endblock %}