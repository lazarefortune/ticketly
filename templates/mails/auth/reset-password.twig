{% extends layout %}

{% import "mails/macros.twig" as element %}

{% block body %}

    {% apply markdown_email %}

        Bonjour {{ user.fullname }} !
        ===============================

        Vous venez de demander la réinitialisation de votre mot de passe sur notre site.
        Pour cela, il vous suffit de cliquer sur le lien ci-dessous :

    {% endapply %}

    {{ element.button('Modifier mon mot de passe', resetUrl|raw , 'primary') }}

    {% apply markdown_email %}
        Si vous n'êtes pas à l'origine de cette demande, vous pouvez ignorer cet email.
    {% endapply %}


    {% set email_link_section %}
        {% apply markdown_email %}
            Si le lien ne fonctionne pas, vous pouvez copier/coller l'adresse suivante dans votre navigateur :
            [ici]({{ resetUrl|raw }})
        {% endapply %}
    {% endset %}


    {{ element.section(email_link_section) }}


{% endblock %}