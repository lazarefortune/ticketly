{% extends 'layouts/organizer/base.html.twig' %}

{% block title %}Coupon - Edition{% endblock %}

{% block body %}
    <div>
        <div>
            <a class="inline-flex items-center gap-2 text-sm/6 text-zinc-500 dark:text-zinc-400"
               href="{{ path('organizer_event_coupons', {'id': event.id}) }}">
                {{ icon('chevron-left') }}
                Retour
            </a>
        </div>

        <div class="mt-6" id="form-container">
            {% include 'pages/organizer/coupons/_form.html.twig' with {'form': form} %}
        </div>
    </div>
{% endblock %}
